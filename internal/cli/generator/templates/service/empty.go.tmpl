// Package {{.PkgName}} implements the {{.Name}} service.
package {{.PkgName}}

import "context"

// Run is the service entry point. It returns when the work is done or ctx is
// cancelled.
func Run(ctx context.Context) error {
	// TODO: implement the service.
	return nil
}
