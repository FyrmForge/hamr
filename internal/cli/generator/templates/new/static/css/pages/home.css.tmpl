.hero {
    text-align: center;
    padding: var(--space-2xl) 0;
}

.hero h1 {
    margin-bottom: var(--space-md);
}

.hero p {
    color: var(--text-secondary);
    font-size: var(--font-size-lg);
    max-width: 600px;
    margin-inline: auto;
}
